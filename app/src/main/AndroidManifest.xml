<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listreminder_mid">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListReminder_Mid">

        <activity
            android:name=".SignupNLogin"
            android:theme="@style/Theme.ListReminder_Mid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Main_Product"
            android:theme="@style/Theme.ListReminder_Mid.NoActionBar"></activity>
        <activity
            android:name=".Web_View"
            android:theme="@style/Theme.ListReminder_Mid.NoActionBar" />
        <activity
            android:name=".Homepage"
            android:theme="@style/Theme.ListReminder_Mid.NoActionBar" />

        <activity android:name=".AddTransaction"
            android:theme="@style/Theme.ListReminder_Mid.NoActionBar"/>

    </application>

</manifest>